# Telegram Bot Token (получить от @BotFather)
BOT_TOKEN=1234567890:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Google GenAI API Key (получить в Google AI Studio)
GEMINI_API_KEY=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Окружение (development | production)
NODE_ENV=production

# PostgreSQL Database URL (локальная разработка)
DATABASE_URL=postgresql://mvz_user:password123@localhost:5432/mvz_bot

# PostgreSQL Database URL (продакшн)
# DATABASE_URL=postgresql://mvz_user:your_secure_password@localhost:5432/mvz_bot

# ID администраторов Telegram (через запятую)
# Получить свой ID можно у бота @userinfobot
# ADMINS=123456789,987654321

# TrueSkill улучшения
# Инфляция рейтинга при неактивности
RATING_IDLE_ENABLED=true
RATING_IDLE_LAMBDA=0.35        # скорость роста неопределенности (в неделю)
RATING_IDLE_PERIOD_DAYS=7      # длина рейтингового периода
RATING_SIGMA0=8.333            # верхний предел неопределенности
RATING_SIGMA_FLOOR=1.0         # нижний предел неопределенности

# MVP бонусы
RATING_MVP_ENABLED=true
RATING_MVP_MU_BONUS=0.6        # бонус к среднему рейтингу MVP
RATING_MVP_SIGMA_MULT=1.0      # множитель неопределенности MVP (0.98 для сжатия)

# Система химии команд и контрпиков
SYNERGY_ENABLED=true           # включить систему химии команд
SYNERGY_WEIGHT_SAME=0.6        # вес синергии в команде (λ_same)
SYNERGY_WEIGHT_VS=0.4          # вес контрпиков между командами (λ_vs)
PAIR_CAP=0.8                   # максимальное значение μ для пар
PAIR_MIN_SAME_GAMES=3          # минимум игр в одной команде для учета синергии
PAIR_MIN_VS_GAMES=3            # минимум игр против друг друга для учета контрпиков
PAIR_GAMES_FOR_CONF=8          # количество игр для максимальной уверенности
PAIR_DECAY_HALF_LIFE_WEEKS=8   # период полураспада в неделях
PAIR_DECAY_FACTOR=0.9          # фактор затухания при устаревании
PAIR_SCALE_SAME=2.0            # масштаб для синергии (SCALE_SAME)
PAIR_SCALE_VS=2.0              # масштаб для контрпиков (SCALE_VS)
MAX_BASE_DIFF=2.0              # максимальная разница в базовой силе команд

# Режим трёх команд (TRI mode)
TRI_MODE_ENABLED=true          # включить режим 3×8 команд
TRI_MINI_MATCH_WEIGHT=0.5      # множитель влияния мини-матча на TrueSkill
TRI_BULK_PARSE_MAX_LINES=100   # максимум строк для парсинга результатов